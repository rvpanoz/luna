/* eslint-disable */

import React from 'react';
import Typography from '@material-ui/core/Typography';

const Audit = props => {
  return <Typography>AUDIT_RESULTS</Typography>;
};

export default Audit;
