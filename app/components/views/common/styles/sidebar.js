const styles = theme => ({
    root: {
        width: '100%',
        display: 'flex',
        flexDirection: 'column',
        height: '100%',
        paddingTop: theme.spacing(11),
    },
    divider: {
        margin: theme.spacing(1, 0)
    }
});

export default styles;
