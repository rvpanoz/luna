const styles = theme => ({
    button: {
        marginLeft: theme.spacing(1)
    },
    root: {
        boxShadow: 'none'
    },
    flexGrow: {
        flexGrow: 1
    }
});

export default styles;
