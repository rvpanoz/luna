/**
 * Footer styles
 */

const styles = (theme) => ({
  root: {
    maxWidth: 600,
    position: 'absolute',
    bottom: 0,
    margin: theme.spacing(1),
    padding: theme.spacing(1),
  },
});

export default styles;
