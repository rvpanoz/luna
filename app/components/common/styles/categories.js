const styles = theme => ({
  root: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'flex-end',
    flexWrap: 'wrap',
    padding: 0
  },
  chip: {
    margin: theme.spacing.unit / 2
  }
});

export default styles;
