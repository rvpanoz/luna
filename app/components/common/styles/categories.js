const styles = (theme) => ({
  root: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'flex-end',
    flexWrap: 'wrap',
    padding: 0,
  },
  chip: {
    margin: theme.spacing(0.5),
  },
});

export default styles;
