const styles = theme => ({
  root: {
    width: '100%'
  },
  textField: {
    margin: 0,
    width: 200
  },
  dense: {
    position: 'relative',
    marginTop: theme.spacing.unit + 2,
    left: '50%'
  }
});

export default styles;
