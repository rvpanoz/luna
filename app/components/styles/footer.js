/**
 * Footer styles
 */

const styles = theme => ({
  root: {
    maxWidth: 600,
    position: 'absolute',
    bottom: 0,
    margin: theme.spacing.unit,
    padding: theme.spacing.unit
  }
});

export default styles;
