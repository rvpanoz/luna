const styles = theme => ({
  root: {
    zIndex: 'auto',
    boxShadow: 'none'
  },
  actions: {
    paddingLeft: theme.spacing(1) / 2
  },
  flexGrow: {
    flexGrow: 1
  },
  button: {
    marginLeft: theme.spacing(1)
  }
});

export default styles;
