/* eslint-disable */

const styles = theme => ({
  wrapper: { position: 'relative', width: '100%' },
  header: {
    position: 'relative',
    zIndex: 4,
    height: 60
  },
  main: {
    width: '100%'
  },
  container: {
    width: '100%',
    padding: theme.spacing.unit * 3
  }
});

export default styles;
