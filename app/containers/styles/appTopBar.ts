import { grayColor } from 'styles/variables';

const styles = () => ({
  root: {
    width: '100%'
  },
  dialogTitle: {
    fontSize: 22,
    color: grayColor
  },
  closeButton: {
    textTransform: 'lowercase'
  }
});

export default styles;
